#!/bin/sh

##
## This file is managed by Puppet; do not edit!
##

while read line;
do
	old=$(echo "$line" | awk '{ print $1 }')
	new=$(echo "$line" | awk '{ print $2 }')
	ref=$(echo "$line" | awk '{ print $3 }')

	case "$ref" in
		*)
			echo "Unrecognised ref '$ref'." >&2
			exit 1
		;;	
	esac
done
